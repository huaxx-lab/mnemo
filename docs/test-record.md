# Test Record：Mnemo 新版本

## 1. 本轮结论

截至 2026-09-01，干净完整自动测试 **136 / 136 通过，0 失败**，Debug 与 Release 构建通过。覆盖引用优先存储、去重与回收站、剪贴板最近五条且无 TTL、自写去回环、上下文意图与安全自动复制、Command-G 跨内容类型 Few-shot 的 `retrieve / answer` 互斥路由、完整证据窗口、双协议输出截断识别、共享 Embedding RAG、候选内模型收敛、有效空 / 格式错误分离、SSE、增量 Markdown、刘海动态几何与点击所有权、归档、待办、番茄钟和热力图。iCloud / CloudKit 已移除。

这不等于全部真实桌面场景已执行。真实 Finder / 触控板 / 刘海屏、外部应用剪贴板、聊天软件文件粘贴和真实供应商凭据仍列在第 5 节；不再包含已删除的双机 CloudKit 验收。

## 2. 环境与命令

| 项 | 值 |
|----|----|
| 日期 | 2026-09-01 |
| 系统 | macOS 27.0（26A5421a） |
| Swift | 6.4（swiftlang-6.4.0.33.1 clang-2100.3.33.1） |
| SDK | MacOSX27.0.sdk |
| Xcode | 27.0 beta（27A5228h），`/Applications/Xcode-beta.app` |
| 测试框架 | swift-testing |
| 测试命令 | `DEVELOPER_DIR=/Applications/Xcode-beta.app/Contents/Developer CLANG_MODULE_CACHE_PATH=/tmp/pinland-module-cache SWIFTPM_MODULECACHE_OVERRIDE=/tmp/pinland-module-cache swift test --scratch-path /tmp/pinland-build` |

最近一次干净运行器结论：`Test run with 136 tests in 0 suites passed after 0.035 seconds.`

## 3. 自动化证据

| 范围 | 已验证行为 | 结果 |
|------|------------|------|
| FileVault 默认策略 | 普通文件默认安全书签；临时来源显式 `.copyRequired`；50MB 旧阈值只在 `.automaticBySize` 迁移入口存在 | ✅ |
| 引用健康状态机 | 确认删除进入回收站；外置卷离线保留并冻结；原文变化保留旧向量并标脏重建 | ✅ |
| 去重与回收站 | 同内容不重复落盘；重复拖入恢复原 Pin ID；向量在回收站冻结；到期后才清理 | ✅ |
| 副本安全 | 空间不足显式失败且无残片；IO 失败回滚；孤儿与缺失副本对账 | ✅ |
| 剪贴板 | 最近 5 条未固定项；无 TTL；固定项不淘汰；检索请求不入库；Mnemo 全部自写 changeCount 去回环 | ✅ |
| AI 方言与能力 | OpenAI 兼容 / Anthropic Messages 编解码；图片输入门控；每功能供应商、模型与思考强度覆盖 | ✅（模拟响应） |
| 模型目录 | 官方 `/models` 原子刷新；失败保留缓存；models.dev 只补元数据，不扩大官方可用列表 | ✅（模拟响应） |
| Embedding | 首次返回向量长度即维度；维度变化检测；维度跨启动持久化；批量响应按 index 复原 | ✅（模拟响应） |
| AI 使用边界 | 敏感内容在网络请求前拦截；场景模型只能排序白名单；低置信度不自动执行危险动作 | ✅（模拟响应） |
| 检索与内容理解 | 主动搜索与剪贴板共享 Embedding / 分块；图片意图；中文二字组；模型最终 ID 删除未选卡；有效空与 malformed 分离 | ✅ |
| 快捷推荐 Agent | Command-G 跨文档、图片、网页、表格、日志、代码和文字的成对 Few-shot；answer 证据不显示文件卡，聚焦输入框时 SSE 直写、否则自动复制；retrieve 唯一安全结果自动复制；未知 ID 丢弃；局部值逐字校验；长页围绕命中取证；双协议 token 上限截断识别；回答卡无叉号、点外收起、自绘悬浮滑块与正文裁剪几何；Command-G 连续触发 latest-wins、无冷却 | ✅（控制器与模拟响应） |
| SSE / Markdown | 两种方言增量、任意网络切块、流式 token 上限截断、`---PINS---` 分隔、稳定 Markdown 块 identity | ✅ |
| 整库归档 | 条目、副本、元数据、待办和专注记录恢复；副本校验失败时不污染目标库 | ✅ |
| 效率模式 | 待办可独立或关联 Pin；绝对时间计时；暂停恢复；13 周热力图聚合 | ✅ |
| 刘海策略 | 拖拽接收深度 148pt，早于系统顶边手势；本地文件 URL 与网页 URL 分流；工作台收起时独立预览继续显示 | ✅（几何与解析层） |
| 授权 | AI 与效率模式经过 entitlement；首装版本只写一次 | ✅ |

## 4. 构建与性能记录

| 项 | 实测结果 | 说明 |
|----|----------|------|
| Debug 构建 | 通过 | 与测试构建共同验证 |
| Release 构建 | 通过 | 已执行打包；临时目录 ad-hoc 包严格验签并启动 |
| 收起空闲物理内存 | 29 MB | Release、空库、Cloud 关闭的隔离实例 |
| 峰值物理内存 | 39 MB | 同一隔离实例 |
| 空闲 CPU / power | 0.0% / 0.0 | 连续 5 次采样 |
| 线程 / 状态 | 4 / sleeping | 空闲时没有轮询忙等 |

旧液体桥与独立拖拽反馈层已移除。先前 29 MB 空闲记录来自较早构建，仅作历史参考；本轮未重新采样性能。展开详情与生成图片 / PDF 预览时内存按需上升，关闭后由系统缓存策略回收，不把预览成本常驻在收起态。

本轮尝试重新抓取独立详情窗口截图时得到黑屏，原因是测试显示环境不可见，因此该截图不作为验收证据，也不据此宣称详情视觉已最终通过。

## 5. 尚需手动或外部验证

| 范围 | 需要的环境 | 当前状态 |
|------|------------|----------|
| 刘海点击与拖放 | 带刘海 Mac、Finder、触控板；反复测试点击、`+` 提示、连续拖入、移出、Mission Control 顶边手势 | 代码完成，待完整实机录屏 |
| 独立详情窗口 | 展开卡片、图片/PDF 预览、文本编辑、未保存关闭确认 | 代码完成，最新视觉复核待执行 |
| 无刘海 / 外接主屏 | 至少一种无刘海或外接主屏组合 | 待手测 |
| 供应商连通性 | 用户重新生成的 MiniMax 与百炼测试 Key | 待外部验证；此前出现在对话中的 Key 按已吊销处理，未写入仓库也不用于测试 |
| `qwen3.7-text-embedding` | 新百炼 Key 与真实 endpoint | 模型名允许手填；真实输出维度待首次调用记录 |
| 微信等应用容器内的文件 | 已授予完全磁盘访问权限的 Mnemo | 未授权时已确认必然失败（TCC），提示与菜单入口已实现；授权后的完整链路待手测 |
| Command-G 快捷推荐 | 浏览器 / PDF 阅读器 / 编辑器选区；文件、网址、解释三类请求；输入框 SSE 直写与焦点改变回退 | 控制器、流式协议和纯策略通过；真实供应商中文完整回答、真实 AX 输入框写入与跨应用读取待验 |
| Keychain 故障路径 | 签名后的真实 App 环境 | 待手测 |
| 验收目录 | 按 `test-strategy.md` 逐条录屏 / 请求计数 / 跨应用记录 | 自动可覆盖部分已通过，其余不虚报 |

## 6. 发布门槛

本轮安装包必须满足：135 项自动测试继续全绿；打包脚本与 ad-hoc 签名通过；应用覆盖 `/Applications/Mnemo.app` 并启动新构建。刘海拖放、外部剪贴板、微信文件粘贴与真实供应商调用若未执行，必须继续列为手测缺口。
